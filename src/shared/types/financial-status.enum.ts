export enum FinancialStatus {
  PENDING = 'PENDING',
  ACTIVE = 'ACTIVE',
  OVERDUE = 'OVERDUE',
  CANCELLED = 'CANCELLED',
  EXEMPT = 'EXEMPT',
}

export enum PlanType {
  PAID = 'PAID',
  SCHOLARSHIP = 'SCHOLARSHIP',
}

export enum PaymentMethod {
  CARD = 'CARD',
  SCHOLARSHIP = 'SCHOLARSHIP',
}
